/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['ka', 'en'],
        defaultLocale: 'ka',
        localeDetection: false,
    },
};

export default nextConfig;
